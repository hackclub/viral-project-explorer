# Development Dockerfile for Go backend with live reload using Air
FROM golang:1.22-alpine

# Install Air for live reload (pinned to v1.52.3 for Go 1.21+ compatibility)
RUN go install github.com/air-verse/air@v1.52.3

WORKDIR /app

# Copy go mod files first for layer caching
COPY go.mod go.sum ./
RUN go mod download

# Copy the rest of the source code
COPY . .

# Expose the API port
EXPOSE 8080

# Run with Air for live reload
CMD ["air", "-c", ".air.toml"]

